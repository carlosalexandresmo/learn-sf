<h1>Posts</h1>

{% for post in posts %}
    <article>
        <h2>{{ post.title }}</h2>
        <small>{{ post.createAt|date('d/m/y') }}</small>
        <div class="content">
            {{ post.content }}
        </div>
    </article>
{% endfor %}